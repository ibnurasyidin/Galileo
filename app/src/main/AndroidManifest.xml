<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.galileo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">

        <activity android:name=".activities.AuthActivity"></activity>
        <activity android:name=".activities.Attivazione3Activity" />
        <activity android:name=".activities.AttivazioneActivity" />
        <activity android:name=".activities.WelcomeScreen" />
        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.HomePageActivity"/>
        <activity android:name=".activities.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".activities.Attivazione2Activity" />
        <activity android:name=".activities.DettaglioSegnalazioniETiicket" />
        <activity android:name=".activities.ScegliITuoiPianiActivity" />

    </application>

</manifest>